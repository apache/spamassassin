#!/usr/bin/perl

use Pod::Html;
use Pod::Text ();

foreach (@ARGV) {
  $in = $_;
  s,^(lib|spamd|spamc)/|\.(pod|pm)$,,g;
  tr,/,_,;

  my $title = $_;
  $title =~ s/_/::/g;
  # convert to HTML: doc/foo.html
  pod2html ("--infile=$in", "--outfile=doc/$_.html", "--css=/assets/style.css", "--title=$title");

  # and to text: doc/foo.txt
  my $parser = Pod::Text->new ();
  $parser->parse_from_file ($in, "doc/$_.txt");
}
