# The recursive chown is performed as an unprivileged user and
# modifies permissions of files generated by sa-compile to match
# expected values.  It is necessary because sa-compile does not
# respect umask.
sa-compile binary: recursive-privilege-change
